

exports.transform = function( element ) {
    var txt = element.text( ".$src > .name" );


    return element;

    /*
    element.className =

    return
    {
        className : element.name,


            artefact : {
        name : ("artefactjs-" + element.type + "-" + element.artefact)
    },
        output : output,
            src: element
    }; */
};