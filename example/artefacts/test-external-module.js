
// var module = require( 'artefactjs-java-EnumObjectPattern' );
var module = require( 'fabrique-log' );

console.log( "module loaded: " + module);

// Used Licenses :
// -----------------------------------------------------------

var mod = require( "artefactjs-java-enumobjpattern" );
console.log(mod);
/*
var licenses = require('licenses');


licenses('arefactsjs-java-enumobjpattern', function fetched(err, license) {
    console.log(license.join(',')); // MIT
});
*/

/*
licenses('fabrique-log', function fetched(err, license) {
    console.log(license.join(',')); // MIT
});
*/

